<%= provide(:title, 'All users') %>
<h1>All users</h1>
<%= will_paginate %>
<ul class="users">
		<%= render @users %>
		<% # user是自定义的视图 rails会自动寻找_user.html.erb，传给render比较特殊，rails会自动遍历这个列表，然后使用_user渲染每个对象 %>
</ul>
<%= will_paginate %>